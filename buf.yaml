version: v2

modules:
  - path: proto

deps:
  - buf.build/bufbuild/protovalidate

lint:
  use:
    - STANDARD
  except:
    - RPC_REQUEST_RESPONSE_UNIQUE
  ignore:
    - proto/buf/validate/validate.proto

breaking:
  use:
    - FILE
