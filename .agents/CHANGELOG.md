# Changelog

## 4.0.0 — 2026-08-08

- Max reasoning is the default invariant; cost optimization may not silently lower reasoning effort.
- Added host-independent modular core plus agent/environment adapters.
- Added transactional generic bootstrap for existing root `AGENTS.md` files.
- Added executable state/evidence/context/module/law/shell infrastructure.
- Added logical cross-process single-subagent lease and no-nested-delegation protocol.
- Added hardened workflow state machine, acceptance gates, hash-chained evidence and workspace-bound final audit.
- Added production-change epochs so later implementation invalidates stale verification and proven gates.
- Added Karpal-inspired declarative law runner and stdlib law helper library.
- Added Codex module for GPT-5.6 Sol / Max with custom roles and V2-aware root+one-child capacity.
- Added first-class Xonsh environment adapter and syntax verifier when Xonsh is installed.
- Added optional mcpyrate/unpythonic extension layer without making recovery depend on them.
- Added content-hash local context ledger and external-source fingerprint/TTL cache.
- Added framework manifest, self-audit, unit tests and safe Codex install/uninstall drift checks.
