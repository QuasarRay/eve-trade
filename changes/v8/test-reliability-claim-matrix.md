# V8 test-reliability claim matrix

Audit baseline: branch `experimental`, HEAD `a04220d11ab6185edd84a3bbc0cebdf9f3cec225`.

This matrix treats `changes/v8/changes.md` as assertions to verify, not as evidence. Statuses reflect the final state of this hardening change. `FIXED_IN_THIS_CHANGE` means the claim gained executable evidence here; it does not imply that adjacent, broader production claims are proven.

| Claim ID | Claim from changes.md | Existing evidence | Gap | Required test/gate | Status |
| -------- | --------------------- | ----------------- | --- | ------------------ | ------ |
| C-001 | Principal-specific HMAC prevents actor spoofing; three actors have distinct credentials. | All actions recursively reject every unbound actor-like field; unknown key, wrong HMAC, 1001/2002/3003 cross-principal attempts, rate-limit identity, fuzz invariant, and live E2E pass. | Production key distribution/rotation remains deployment-specific. | Retain the unit, fuzz, and production-gate E2E matrix. | FIXED_IN_THIS_CHANGE |
| C-002 | Rust settlement has meaningful validation, arithmetic, coverage, clippy, and audit gates. | Seventeen unit/property tests and CI commands exist. | Most transactional correctness is proved only by Python E2E; Rust coverage threshold is low and does not exercise a real database. | Keep Rust gates and add database-backed rollback/idempotency/boundary proof. | PARTIALLY_PROVEN |
| C-003 | Dagger runs the canonical Quilkin/simulator topology with production-gate E2E and no skips. | Equivalent canonical Compose production gate passed 130 tests with zero skips; Dagger contains the same gate. | Dagger itself was not executable in this environment. | Execute the Dagger entry point in CI and retain zero-skip enforcement. | PARTIALLY_PROVEN |
| C-004 | Every GUI assertion and severe health/log finding makes the runner fail. | Negative runner contract injects browser failure, unhealthy/exited/restarted containers, panic, fatal, OOM, unhandled exception, and stack trace; all gate nonzero. | Full browser flow was not rerun here. | Retain contract test and canonical browser gate. | FIXED_IN_THIS_CHANGE |
| H-001 | Hostile price/item/station/seller facts are sent and canonical state wins. | Helpers put hostile facts on the wire; live E2E proves canonical price/item/station state and rejects a conflicting seller actor at the authenticated edge. | None for the stated hostile fields. | Retain wire and DB assertions. | FIXED_IN_THIS_CHANGE |
| H-002 | Exact over-release and over-payment semantics are tested. | Live low-level settlement E2E rejects item escrow over-release and wallet escrow over-payment with exact codes/messages, then proves wallets, item escrow, trade state, escrow rows, and both ledgers fully rolled back. | None for these two failure classes. | Retain exact before/after snapshots. | FIXED_IN_THIS_CHANGE |
| H-003 | SQL tests are named as persisted-state evidence, not UI evidence. | Test names generally describe database state; UI tests are separate. | Naming cannot prove user-visible behavior. | Static naming guard plus API/GUI assertions for user-visible claims. | PROVEN |
| H-004 | Forced-overlap concurrency has exact loser code and conserved final state. | Barrier-driven accept/accept and accept/cancel tests assert one winner, exact loser, final state, balances, quantities, escrow, and ledger conservation. A discovered PostgreSQL deadlock was fixed with ordered transaction advisory locks; accept/cancel passed 10 forced repeats. | Cross-process/database-failover races remain outside local scope. | Retain repeated live E2E concurrency gate. | FIXED_IN_THIS_CHANGE |
| H-005 | Every `expect_rpc_error` call asserts exact code and relevant message. | Helper requires both values and an AST gate rejects missing or empty literal `code`/`contains` arguments. | None in the checked E2E tree. | Retain semantic architecture gate. | FIXED_IN_THIS_CHANGE |
| H-006 | Falsey helper values reach production code unchanged. | Meta-test proves `0`, `""`, `false`, and `[]` remain in emitted payloads. | None in covered helpers. | Retain wire-payload regression. | FIXED_IN_THIS_CHANGE |
| H-007 | Production-gate mode turns every E2E skip into failure everywhere. | Production mode fails immediately instead of skipping, even when the non-production all-skipped escape hatch is set; meta-tests remove every dependency/credential in turn; live run passed 130 with zero skips. | Conditional future suites must add their dependencies to the contract list. | Retain session hook, immediate failure helper, and dependency matrix. | FIXED_IN_THIS_CHANGE |
| H-008 | Duplicate E2E block and stale catalog were removed. | Collection has no named duplicate block; README points to collection. | Test-count independence still requires review, not a gate. | Duplicate-node-id collection guard. | PROVEN |
| H-009 | Real UDP listener, shutdown/drain, size/queue limits, config, signatures, and principal rate limits are tested. | Real listener tests prove deterministic oversize/overflow and admitted-job drain; config rejects malformed critical values; signatures and rate limits are principal-bound. | Kernel-level packet loss/capacity remains environment-dependent. | Retain listener and live burst gates. | FIXED_IN_THIS_CHANGE |
| H-010 | Replay/cache/lost-response/transient retry and durable Market replay are proven. | Gateway cache/replay tests and E2E settlement-count checks exist. | Process restart/multi-replica durability and all response-class correlations are absent. | Durable replay across service restart and response identity checks for errors. | PARTIALLY_PROVEN |
| H-011 | Live RabbitMQ contract covers confirms, delivery/rejection, metadata, acks, unroutable replies, and graceful drain. | Live broker test observes metadata/correlation, success/rejection, publisher confirms, malformed dead-letter/nack, unroutable reply ack, and queued work released after cancellation; Linux race run passed. | Broker failover/network partition is not covered. | Retain required live-broker CI gate. | FIXED_IN_THIS_CHANGE |
| H-012 | Settlement executor readiness cannot pass without dependency health. | Exact nil dependency and failed `Ping` tests fail readiness; live worker uses the same readiness path. | None for the stated claim. | Retain worker readiness tests. | FIXED_IN_THIS_CHANGE |
| H-013 | Production app services use a restricted runtime DB role; privileged credentials are migration-only. | Compose and Kubernetes now separate migration/runtime secret references; live role test proves exact grants and forbidden operations. | EKS/GKE Terraform still builds `trade-settlement-database` from the managed database administrator credential, so the production-wide claim remains false. | Provision a separate managed runtime role and secret in each Terraform root, then test rendered secret wiring without exposing values. | FALSE_OR_OVERSTATED |
| H-014 | Item and wallet ledgers have symmetric update/delete immutability tests. | Runtime-role E2E directly proves update and delete denial for both ledgers. | None for the stated operations. | Retain runtime-role attempts. | FIXED_IN_THIS_CHANGE |
| H-015 | Migration harness proves rerun preservation and runtime grants; Compose/Dagger use it. | Harness reapplies migrations, preserves data, installs an exact table privilege allowlist, and compares actual grants; canonical Compose runs it. | Managed-cloud runtime role provisioning remains unresolved by H-013. | Retain harness everywhere migrations run. | FIXED_IN_THIS_CHANGE |
| H-016 | Pull requests run Buf breaking plus lint/format/generation/build/drift. | Workflow contains these required commands. | Requires CI execution against an actual PR base to prove behavior. | Keep required job and validate workflow syntax. | PROVEN |
| H-017 | CI and Dagger run strict kubeconform without missing-schema bypass. | Both commands omit `-ignore-missing-schemas`; the production structural verifier passes. | Actual kubeconform run fails because the pinned CRD catalog has no `IstioOperator` or `ChaosEngine` schema. The claimed successful strict schema gate is not real. | Supply authoritative pinned custom-resource schemas or validate those resources with their authoritative tools while keeping kubeconform strict for the rest. | FALSE_OR_OVERSTATED |
| H-018 | Chaos uses authenticated probes before/during/after disruption and requires Litmus pass. | Dagger script and Litmus manifests include probe hooks/verdict checks. | Bearer token may be empty; continuous probe does not prove a successful request occurred during disruption; no real cluster execution. | Require token, timestamp/count successful in-disruption probes, and retain external-cluster gate. | PARTIALLY_PROVEN |
| H-019 | Deployment requires three accepted authenticated external trades and rolls back on failure. | Deploy script loops three authenticated requests and checks accepted text. | Empty token is allowed; responses need not be distinct/committed and are validated by text rather than structured state. | Require token, unique request IDs, structured responses, and backend commitment evidence. | PARTIALLY_PROVEN |
| H-020 | Browser GUI gate starts canonical topology, fails on runner checks, and uploads evidence. | Negative runner contract covers every failure class; the canonical browser run passed 57/57 assertions, emitted screenshots/video/results, and found zero severe log, health, exit, or restart issues. | CI artifact upload itself was not invoked locally. | Retain contract, browser gate, and required artifact upload. | FIXED_IN_THIS_CHANGE |
| H-021 | Claims are limited to repository-protocol conformance with shared schema/golden packet. | Django and Go consume the schema/golden fixture. | Cross-language canonical signing is not independently checked against multiple vectors. | Add shared request/response signing vectors. | PROVEN |
| H-022 | Every UDP response is canonical, signed, source-bound, and interaction-bound. | MAC binds algorithm, key ID, protocol version, status/code/message/body and interaction ID; client tests reject wrong source/key/body/version/interaction across success/business/transient/malformed classes and replay. Live multi-client burst exposed and then verified the Quilkin session-race mitigation. | Quilkin is constrained to one ingress worker until its upstream socket reservation race is fixed; horizontal capacity is unmeasured. | Retain adversarial response matrix and live multi-client burst. | FIXED_IN_THIS_CHANGE |
| H-023 | Authenticated UDP burst gate proves p95, exact acceptance, and conservation. | E2E load test sends at least ten independent requests and checks counts/totals. | It is a short burst, not soak/capacity evidence; external resource contention is limited. | Keep scope explicitly burst-only or add sustained soak/capacity gate. | PARTIALLY_PROVEN |
| H-024 | Go fuzz and Rust properties cover principal binding and signed-64 boundaries. | Fuzz invariant inspects every recursive actor claim; bounded run passed 92,164 executions. Rust tests cover checked arithmetic plus explicit i64 minimum, zero, and maximum validation. | Fuzz/corpus depth is bounded and Rust database operations do not execute every boundary. | Retain bounded fuzz and add database-backed boundary cases. | FIXED_IN_THIS_CHANGE |
| M-001 | Coverage thresholds/artifacts exist for Go, Rust, simulator, and observability. | Per-module Go, Rust, simulator, observability and E2E branch-coverage gates upload artifacts; local results were Go 28.5%-72.7% by tested module, simulator 84%, observability 35%, E2E 93%. | Rust 20% and observability 35% thresholds remain weak and cannot support broad correctness claims. | Raise thresholds by adding meaningful tests, especially observability orchestration and Rust database paths. | PARTIALLY_PROVEN |
| M-002 | All observability tests execute with strict coverage in both CI systems. | Both workflows invoke pytest with coverage. | Needs execution in this change. | Run observability suite under configured coverage. | PARTIALLY_PROVEN |
| M-003 | Simulator covers retry completion/authentication/exhaustion/source and identical payloads. | Thirteen tests cover completion, exhaustion, source, exact retry bytes, signatures, tampered body/version, wrong key, malformed JSON, and wrong/missing interaction for every response class. | None for the stated unit-level claim. | Retain the adversarial table. | FIXED_IN_THIS_CHANGE |
| M-004 | GUI refresh requires exactly one committed backend interaction; zero cannot pass. | Runner checks equality to one. | No isolated regression test proves zero and two both fail. | Negative runner contract tests. | PARTIALLY_PROVEN |
| M-005 | Fatal scan covers panic/fatal/OOM/unhandled/stack trace/health/exit/restart and fails. | Injected runner contract covers every listed class and passed. | None in the contract layer; full browser run remains V-004. | Retain the contract test before GUI execution. | FIXED_IN_THIS_CHANGE |
| M-006 | Boundary guards use AST/assertion and rendered-resource structure, not markers. | Architecture guard now enforces exact E2E error assertions structurally; Compose and Kubernetes verifiers parse rendered JSON/YAML. | Static guards cannot judge all assertion semantics. | Retain static guards alongside live gates. | FIXED_IN_THIS_CHANGE |
| M-007 | Production image gate structurally requires valid digest references. | Rendered production workloads are parsed and exact `name@sha256:<64 hex>` references are required; local production render passed. | Custom-resource schema validation remains broken under H-017. | Retain structural image policy and add verifier unit mutations. | FIXED_IN_THIS_CHANGE |
| M-008 | Actions/runtimes/dependencies/providers/base images are reproducibly pinned and Go builds offline. | Many commit/digest/exact pins, Cargo lock, vendor tree, and drift gates exist. | Terraform lock files are absent; some CI tool images are tag-only; Python transitive dependencies lack hashes. | Provider locks, digest-pinned tool images, hashed Python lock files. | PARTIALLY_PROVEN |
| M-009 | CI performs full-history secrets, vulnerability scans, audits, final-image scans, and SBOM export. | Commands exist in both CI implementations. | Local execution and generated artifact verification are absent; scanner images are not digest pinned. | Execute scanners in CI and verify artifacts/provenance. | PARTIALLY_PROVEN |
| M-010 | Dagger has parity with GitHub for all listed reliability/security gates. | `pipeline.py` contains matching broad gate families. | E2E Python coverage and some artifact behavior differ; parity is not machine-compared. | Gate manifest/meta-test comparing required capabilities. | PARTIALLY_PROVEN |
| M-011 | Jobs have timeouts and concurrency/load E2E repeats three times without early maxfail. | Workflow has job timeouts and repeat invocation. | Dagger timeout parity is incomplete. | Execute repeat gate and enforce per-stage time bounds in both systems. | PARTIALLY_PROVEN |
| M-012 | Function-scoped HTTP clients are closed. | `GatewayClient.close()` and fixture finalizer exist. | No leak regression test. | Unit test close call or context-managed fixture. | PROVEN |
| M-013 | Stale catalogs removed; GUI evidence records provenance and historical artifacts are labeled non-gating. | Files and runner fields show these changes. | Checked-in historical artifacts can still be mistaken for fresh evidence outside the explanatory files. | Generated evidence manifest with explicit gating status and commit match. | PARTIALLY_PROVEN |
| M-014 | Terraform tests assert representative production topology, protection, and deployment inputs. | Tests now inspect concrete RDS/Cloud SQL/network/secret/in-cluster topology attributes. | Third-party `kubectl` cannot be represented by Terraform's default mock-provider source; invalid mocks were removed, but provider/module registry access failed locally before plans executed. H-013 also remains unresolved. | Execute provider-backed tests in CI and add runtime/migration credential separation assertions. | PARTIALLY_PROVEN |
| M-015 | Release gates cross production ingress/policy via digest render, authenticated smoke, and chaos probes. | Structural gates and external probe scripts exist. | Functional probes are insufficiently identity/commitment-bound and unexecuted here. | Unique signed trade probes with backend commitment through real ingress. | PARTIALLY_PROVEN |
| M-016 | Rust/E2E cover int64, overflow, UUID, consistency, checksum, over-release/payment, and ledger mutation. | Unit/property and E2E cases exist. | Minimum/maximum values are not exercised through the database API; exact rollback/conservation is incomplete. | Database-backed min/max/overflow and exact rollback state assertions. | PARTIALLY_PROVEN |
| M-017 | Readiness requires executor ping/nil failure; deploy/chaos require functional trades. | Executor ping contract and health tests exist; external scripts issue HTTP probes. | Rust settlement readiness is TCP-only; nil Rabbit executor lacks direct test; trade probes are not commitment-bound. | Functional readiness transaction/ping and exact nil/probe tests. | PARTIALLY_PROVEN |
| M-018 | API listener and Rabbit worker cancellation drain queued work. | Both tests block admitted work, cancel, release, and assert all admitted jobs complete; live Rabbit drain passed. | Process kill beyond graceful cancellation is outside this claim. | Retain blocked-work drain tests. | FIXED_IN_THIS_CHANGE |
| A-001 | Simulator maps UDP business errors to precise non-2xx HTTP, persists failure, and tests HTTP 400 invalid argument. | HTTP 202 now requires exact `accepted`; business/invalid/unknown failures map non-2xx and persist failed interaction state. | None for covered response classes. | Retain Django mapping/persistence tests. | FIXED_IN_THIS_CHANGE |
| A-002 | Browser parser preserves actionable malformed-JSON/client-validation errors. | Canonical browser run asserted the exact client-visible malformed-extra-JSON message and passed. | The assertion remains in the broader browser suite rather than a small DOM unit test. | Retain the browser assertion; a focused DOM test would improve diagnosis. | FIXED_IN_THIS_CHANGE |
| A-003 | Load requests use independent canonical one-unit stacks. | Live production-gate burst passed and verified exact accepted count, independent stacks, p95 budget, and DB conservation; repeated five times after proxy mitigation. | This remains burst evidence, not soak/capacity evidence. | Retain burst and add sustained capacity test before capacity claims. | FIXED_IN_THIS_CHANGE |
| A-004 | Capsuleer 3003 has a distinct credential for contention/negative paths. | Production-gate requires third-principal credentials and live E2E proves 3003 cannot act as buyer/seller without matching actor. | External secret rotation remains deployment-specific. | Retain production credential contract. | FIXED_IN_THIS_CHANGE |
| A-005 | Dagger Gitleaks scans full history. | `.git` is mounted and command omits `--no-git`; GitLab depth is zero. | Needs CI execution. | Run full-history Gitleaks and fail on findings/tool failure. | PARTIALLY_PROVEN |
| V-001 | Fresh-volume production-gate Compose E2E passes 104 tests with zero skips. | Current canonical production-gate run passed 130 tests, zero skipped, with 93% branch coverage. | Historical count 104 is stale. | Update validation claim to the current result. | FIXED_IN_THIS_CHANGE |
| V-002 | Three-pass concurrency/load repeat passes. | The exact configured command passed 12 selected repetitions with 375 deselected; accept/cancel also passed 10 focused repeats and the burst passed five focused repeats. | None for the configured local repeat gate. | Retain the exact repeat command in CI. | FIXED_IN_THIS_CHANGE |
| V-003 | Django simulator passes nine tests. | Current suite passed 13 tests with 84% branch coverage. | Historical count nine is stale. | Update validation claim to the current result. | FIXED_IN_THIS_CHANGE |
| V-004 | Headless GUI gate passes 57 checks with clean fatal scan. | Fresh canonical run passed 57/57, emitted evidence, and reported zero fatal/OOM, unhealthy, exited, or restarted containers. | None for this local browser gate. | Retain provenance matching and CI artifact upload. | FIXED_IN_THIS_CHANGE |
| V-005 | Rust tests/format pass. | `cargo fmt --check`, 19 locked all-feature tests, and clippy with warnings denied passed. | Rust database coverage remains low. | Retain all three gates. | FIXED_IN_THIS_CHANGE |
| V-006 | Python observability passes fourteen tests. | Fourteen tests passed at the configured 35% branch threshold. | Calling 35% strict is overstated. | Raise coverage with meaningful orchestration tests. | PARTIALLY_PROVEN |
| V-007 | Workspace Go tests and module coverage pass. | Root tests passed; module coverage passed at 44.4%, 28.5%, 33.9%, 42.9%, and 68.7%; live Rabbit contract passed under Linux `-race`. | Full all-module race suite was not run locally. | Retain Linux matrix race gates. | FIXED_IN_THIS_CHANGE |
| V-008 | API fuzz passes bounded high-volume runs. | Strengthened target passed 10 seconds with two workers and 92,164 executions after host memory was bounded. | A default 16-worker local run exhausted host memory. | Set an explicit practical worker budget in developer commands/CI if runners are constrained. | FIXED_IN_THIS_CHANGE |
| V-009 | E2E collection and error-helper strictness pass. | 129 tests collected/executed; AST meta-gate enforces exact code and message arguments. | None for the claim. | Retain collection/meta gate. | FIXED_IN_THIS_CHANGE |
| V-010 | Compose/YAML/Terraform/architecture static validation passes. | Compose renders/credential policy, workflow YAML, architecture guard, Kubernetes renders, structural production policy, GUI contract, and Terraform format passed. | Kubeconform fails on missing custom schemas; Terraform init/test is blocked by registry access after invalid third-party mocks were removed. The umbrella claim is false. | Fix H-017 and execute provider-backed Terraform tests. | FALSE_OR_OVERSTATED |
| V-011 | Production Go images build offline from vendor. | Historical statement and Dockerfiles/vendor gates. | Not rebuilt in this change. | Build all production Go images with network-independent module resolution. | UNPROVEN |
