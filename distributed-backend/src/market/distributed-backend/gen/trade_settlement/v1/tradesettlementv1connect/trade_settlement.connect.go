// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: trade_settlement/v1/trade_settlement.proto

package tradesettlementv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/astral/eve-trade/market/distributed-backend/gen/trade_settlement/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// TradeSettlementServiceName is the fully-qualified name of the TradeSettlementService service.
	TradeSettlementServiceName = "eve.trade_settlement.v1.TradeSettlementService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// TradeSettlementServiceExecuteSettlementBatchProcedure is the fully-qualified name of the
	// TradeSettlementService's ExecuteSettlementBatch RPC.
	TradeSettlementServiceExecuteSettlementBatchProcedure = "/eve.trade_settlement.v1.TradeSettlementService/ExecuteSettlementBatch"
)

// TradeSettlementServiceClient is a client for the eve.trade_settlement.v1.TradeSettlementService
// service.
type TradeSettlementServiceClient interface {
	ExecuteSettlementBatch(context.Context, *connect.Request[v1.ExecuteSettlementBatchRequest]) (*connect.Response[v1.ExecuteSettlementBatchResponse], error)
}

// NewTradeSettlementServiceClient constructs a client for the
// eve.trade_settlement.v1.TradeSettlementService service. By default, it uses the Connect protocol
// with the binary Protobuf Codec, asks for gzipped responses, and sends uncompressed requests. To
// use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or connect.WithGRPCWeb()
// options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewTradeSettlementServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) TradeSettlementServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	tradeSettlementServiceMethods := v1.File_trade_settlement_v1_trade_settlement_proto.Services().ByName("TradeSettlementService").Methods()
	return &tradeSettlementServiceClient{
		executeSettlementBatch: connect.NewClient[v1.ExecuteSettlementBatchRequest, v1.ExecuteSettlementBatchResponse](
			httpClient,
			baseURL+TradeSettlementServiceExecuteSettlementBatchProcedure,
			connect.WithSchema(tradeSettlementServiceMethods.ByName("ExecuteSettlementBatch")),
			connect.WithClientOptions(opts...),
		),
	}
}

// tradeSettlementServiceClient implements TradeSettlementServiceClient.
type tradeSettlementServiceClient struct {
	executeSettlementBatch *connect.Client[v1.ExecuteSettlementBatchRequest, v1.ExecuteSettlementBatchResponse]
}

// ExecuteSettlementBatch calls
// eve.trade_settlement.v1.TradeSettlementService.ExecuteSettlementBatch.
func (c *tradeSettlementServiceClient) ExecuteSettlementBatch(ctx context.Context, req *connect.Request[v1.ExecuteSettlementBatchRequest]) (*connect.Response[v1.ExecuteSettlementBatchResponse], error) {
	return c.executeSettlementBatch.CallUnary(ctx, req)
}

// TradeSettlementServiceHandler is an implementation of the
// eve.trade_settlement.v1.TradeSettlementService service.
type TradeSettlementServiceHandler interface {
	ExecuteSettlementBatch(context.Context, *connect.Request[v1.ExecuteSettlementBatchRequest]) (*connect.Response[v1.ExecuteSettlementBatchResponse], error)
}

// NewTradeSettlementServiceHandler builds an HTTP handler from the service implementation. It
// returns the path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewTradeSettlementServiceHandler(svc TradeSettlementServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	tradeSettlementServiceMethods := v1.File_trade_settlement_v1_trade_settlement_proto.Services().ByName("TradeSettlementService").Methods()
	tradeSettlementServiceExecuteSettlementBatchHandler := connect.NewUnaryHandler(
		TradeSettlementServiceExecuteSettlementBatchProcedure,
		svc.ExecuteSettlementBatch,
		connect.WithSchema(tradeSettlementServiceMethods.ByName("ExecuteSettlementBatch")),
		connect.WithHandlerOptions(opts...),
	)
	return "/eve.trade_settlement.v1.TradeSettlementService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case TradeSettlementServiceExecuteSettlementBatchProcedure:
			tradeSettlementServiceExecuteSettlementBatchHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedTradeSettlementServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedTradeSettlementServiceHandler struct{}

func (UnimplementedTradeSettlementServiceHandler) ExecuteSettlementBatch(context.Context, *connect.Request[v1.ExecuteSettlementBatchRequest]) (*connect.Response[v1.ExecuteSettlementBatchResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("eve.trade_settlement.v1.TradeSettlementService.ExecuteSettlementBatch is not implemented"))
}
