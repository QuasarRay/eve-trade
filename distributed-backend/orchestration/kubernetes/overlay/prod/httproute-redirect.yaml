apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: encore-backend-http-redirect
  labels:
    app.kubernetes.io/name: encore-backend
    app.kubernetes.io/component: backend
spec:
  parentRefs:
    - name: public-web
      namespace: eve-trade-gateway
      sectionName: http
  hostnames:
    - api.eve-trade.example.com
  rules:
    - filters:
        - type: RequestRedirect
          requestRedirect:
            scheme: https
            statusCode: 301
