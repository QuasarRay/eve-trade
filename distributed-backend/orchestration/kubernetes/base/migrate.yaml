apiVersion: batch/v1
kind: Job
metadata:
  name: settlement-db-migrate
  labels:
    app.kubernetes.io/name: settlement-db-migrate
    app.kubernetes.io/component: migration
spec:
  backoffLimit: 3
  ttlSecondsAfterFinished: 3600
  template:
    metadata:
      labels:
        app.kubernetes.io/name: settlement-db-migrate
        app.kubernetes.io/component: migration
    spec:
      serviceAccountName: trade-settlement
      automountServiceAccountToken: false
      enableServiceLinks: false
      restartPolicy: OnFailure
      securityContext:
        runAsNonRoot: true
        runAsUser: 999
        runAsGroup: 999
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: migrate
          image: postgres:16
          imagePullPolicy: IfNotPresent
          envFrom:
            - secretRef:
                name: trade-settlement-database
          command:
            - /bin/sh
            - -ec
          args:
            - |
              if psql "$DATABASE_URL" -tAc "SELECT to_regclass('public.trade_instance')" | grep -q trade_instance; then
                echo 'Database schema already exists; applying compatible migrations.';
                psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f /migrations/0002_merge_item_stack_constraints.sql;
              else
                psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f /migrations/0001_settlement_schema.sql;
                psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f /migrations/0002_merge_item_stack_constraints.sql;
              fi
          volumeMounts:
            - name: migrations
              mountPath: /migrations
              readOnly: true
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 256Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
      volumes:
        - name: migrations
          configMap:
            name: settlement-db-migrations
