[pytest]
addopts = -ra
testpaths = .
markers =
    live: requires running PostgreSQL and one or more microservices
    settlement: exercises the trade-settlement service
    market: exercises the market service
    gateway: exercises the api-gateway service
    contract: validates local contracts without live services
