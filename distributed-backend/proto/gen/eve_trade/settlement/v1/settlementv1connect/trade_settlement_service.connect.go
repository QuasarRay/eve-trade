// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: eve_trade/settlement/v1/trade_settlement_service.proto

package settlementv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/QuasarRay/eve-trade/distributed-backend/proto/gen/eve_trade/settlement/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// TradeSettlementServiceName is the fully-qualified name of the TradeSettlementService service.
	TradeSettlementServiceName = "eve_trade.settlement.v1.TradeSettlementService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// TradeSettlementServiceStreamTradeSettlementCommandsProcedure is the fully-qualified name of the
	// TradeSettlementService's StreamTradeSettlementCommands RPC.
	TradeSettlementServiceStreamTradeSettlementCommandsProcedure = "/eve_trade.settlement.v1.TradeSettlementService/StreamTradeSettlementCommands"
)

// TradeSettlementServiceClient is a client for the eve_trade.settlement.v1.TradeSettlementService
// service.
type TradeSettlementServiceClient interface {
	StreamTradeSettlementCommands(context.Context) *connect.BidiStreamForClient[v1.StreamTradeSettlementCommandsRequest, v1.StreamTradeSettlementCommandsResponse]
}

// NewTradeSettlementServiceClient constructs a client for the
// eve_trade.settlement.v1.TradeSettlementService service. By default, it uses the Connect protocol
// with the binary Protobuf Codec, asks for gzipped responses, and sends uncompressed requests. To
// use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or connect.WithGRPCWeb()
// options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewTradeSettlementServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) TradeSettlementServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	tradeSettlementServiceMethods := v1.File_eve_trade_settlement_v1_trade_settlement_service_proto.Services().ByName("TradeSettlementService").Methods()
	return &tradeSettlementServiceClient{
		streamTradeSettlementCommands: connect.NewClient[v1.StreamTradeSettlementCommandsRequest, v1.StreamTradeSettlementCommandsResponse](
			httpClient,
			baseURL+TradeSettlementServiceStreamTradeSettlementCommandsProcedure,
			connect.WithSchema(tradeSettlementServiceMethods.ByName("StreamTradeSettlementCommands")),
			connect.WithClientOptions(opts...),
		),
	}
}

// tradeSettlementServiceClient implements TradeSettlementServiceClient.
type tradeSettlementServiceClient struct {
	streamTradeSettlementCommands *connect.Client[v1.StreamTradeSettlementCommandsRequest, v1.StreamTradeSettlementCommandsResponse]
}

// StreamTradeSettlementCommands calls
// eve_trade.settlement.v1.TradeSettlementService.StreamTradeSettlementCommands.
func (c *tradeSettlementServiceClient) StreamTradeSettlementCommands(ctx context.Context) *connect.BidiStreamForClient[v1.StreamTradeSettlementCommandsRequest, v1.StreamTradeSettlementCommandsResponse] {
	return c.streamTradeSettlementCommands.CallBidiStream(ctx)
}

// TradeSettlementServiceHandler is an implementation of the
// eve_trade.settlement.v1.TradeSettlementService service.
type TradeSettlementServiceHandler interface {
	StreamTradeSettlementCommands(context.Context, *connect.BidiStream[v1.StreamTradeSettlementCommandsRequest, v1.StreamTradeSettlementCommandsResponse]) error
}

// NewTradeSettlementServiceHandler builds an HTTP handler from the service implementation. It
// returns the path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewTradeSettlementServiceHandler(svc TradeSettlementServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	tradeSettlementServiceMethods := v1.File_eve_trade_settlement_v1_trade_settlement_service_proto.Services().ByName("TradeSettlementService").Methods()
	tradeSettlementServiceStreamTradeSettlementCommandsHandler := connect.NewBidiStreamHandler(
		TradeSettlementServiceStreamTradeSettlementCommandsProcedure,
		svc.StreamTradeSettlementCommands,
		connect.WithSchema(tradeSettlementServiceMethods.ByName("StreamTradeSettlementCommands")),
		connect.WithHandlerOptions(opts...),
	)
	return "/eve_trade.settlement.v1.TradeSettlementService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case TradeSettlementServiceStreamTradeSettlementCommandsProcedure:
			tradeSettlementServiceStreamTradeSettlementCommandsHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedTradeSettlementServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedTradeSettlementServiceHandler struct{}

func (UnimplementedTradeSettlementServiceHandler) StreamTradeSettlementCommands(context.Context, *connect.BidiStream[v1.StreamTradeSettlementCommandsRequest, v1.StreamTradeSettlementCommandsResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("eve_trade.settlement.v1.TradeSettlementService.StreamTradeSettlementCommands is not implemented"))
}
